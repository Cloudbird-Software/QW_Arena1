# src/strategy —— 策略说明文档模块（IR-0001 W8）

## 职责

生成随产物包落盘的策略说明文档：单商品成本估算、与人工流程的效率对比、
换品类与换市场适配成本声明三节（BEH-8），数据引用运行日志可追溯。

## 不变量

- 三节结构是可枚举资产（STRATEGY_SECTIONS）——禁止在渲染逻辑外散落节定义。
- 文档内容由输入数据渲染（数据与运行日志可追溯），不含硬编码业务数字。

## 禁止

- 不做成本计算本身（估算数据由管线运行时统计注入）。

## 独立验证

`make test tests/card-13-w8-strategy-document.test.ts`
