---
taskId: IR-0001
specVersion: 1
title: 视觉质量优先——品类构图×市场词典×VLM 质检闭环×降级收缩×商业叙事
irRef: IR-0001
card: "Cloudbird-Software/QW_Arena1#2"
acceptanceCriteria:
  - id: AC-1
    given: 品类构图模板映射与图像角色清单已定义
    when: 为任一品类与角色的组合组装图像提示词
    then: 提示词含该品类专属构图指令，任取两个不同品类得到的构图指令不同，且映射以产物包内可枚举资产存在
  - id: AC-2
    given: 三市场视觉风格词典已定义
    when: 目标市场分别取 EN、KO、PT 组装图像提示词
    then: 三份提示词分别注入对应市场的光照、色调与风格条目，且三组条目两两互异
  - id: AC-3
    given: 一张模型生成的图片已产出
    when: 质检环节对该图执行四维评审
    then: 产出覆盖事实一致性、构图、技术质量、合规四维的机器可解析评审记录，判定不合格时携带评审反馈重生成，单图重生成次数不超过 BUDGET-2
  - id: AC-4
    given: 相同商品输入与相同生成参数
    when: 图像生成调用发起
    then: 调用携带固定种子与负面提示词，负面提示词覆盖图内文字、水印、拼图、边框四类失败模式
  - id: AC-5
    given: 目标市场已确定且三语字体已打包
    when: 尺码表详情图产出
    then: 该图由本地渲染生成且全程零模型调用
  - id: AC-6
    given: 图像或视频生成主路径失败
    when: 兜底逻辑接管该素材的生成
    then: 图片兜底为源图裁剪加本地合成、视频兜底为图片轮播，且产物包内不存在多级模型轮换降级路径
  - id: AC-7
    given: 全部图片产物已落盘待检
    when: 规格校验对落盘产物运行
    then: 主图为白底方图且最短边不低于 1200 像素、详情图最短边不低于 900 像素、单张大小不超过 4.5MB
  - id: AC-8
    given: 策略说明文档内容定稿
    when: 文档随产物包落盘
    then: 文档含单商品成本估算、与人工流程的效率对比、换品类与换市场适配成本声明三节
  - id: AC-9
    given: 本 spec 与套件已合并
    when: 红队审计运行并产出判定
    then: 存在 verdict 为 survived 的审计记录，且后续工作卡的验收标准逐条派生自本 spec 的 AC-1 至 AC-8
nonGoals:
  - 不重写 Plan.md 已定整体架构（流水线阶段划分、时间预算管理器、溯源门保留）
  - 不做多商品批量模式、A/B 测试框架、品牌风格学习（属赛后演进）
  - 不追求全部 12 个 A/B 实验，聚焦命名格式验证、全流程计时、主图质量、A1 合规覆盖
  - 不改动本仓治理脚手架（quality 关卡、CI 工作流、Makefile 检查目标）
blastRadius:
  - "Cloudbird-Software/QW_Arena1: specs/IR-0001/**"
  - "Cloudbird-Software/QW_Arena1: src/visual/**（实现卡落地时的视觉管线模块）"
---

# IR-0001 视觉质量优先（视觉管线条款级规格）

## INV 不变量

- INV-1: 视觉质量三核心件——品类构图模板、三市场视觉词典、VLM 质检闭环——不得被降级路径替换或绕过。
- INV-2: 主图与展示商品本体的详情图必须以源商品图为参考锚定（参考图生成模式）；场景类详情图允许纯文生图，但须同时绑定品类构图与市场词典。
- INV-3: 全部视觉控制点（构图模板、市场词典、负面提示词、规格阈值、质检反馈）必须以可枚举的配置资产或代码规则存在，禁止散落于不可追溯的自由文本——这是把美学偏好沉淀为可复用工程资产、并使视觉质量可度量可回归的前提。
- INV-4: 比赛硬约束在任何视觉质量增强中不得突破——墙钟不超过 30 分钟、内存不超过 4GB、提交包不超过 100MB、仅白名单内 36 个模型、中间产物只走模型到 URL 再到模型的链路、密钥只从环境变量读取、产物命名严格校验。

## BEH 行为

- BEH-1: 当服装品类判定完成时，系统必须为每个图像角色选取该品类对应的构图模板并注入提示词。
- BEH-2: 当目标市场为 EN、KO、PT 之一时，系统必须将该市场的光照、色调与风格词典条目注入图像提示词。
- BEH-3: 当任一生成图被质检判定为不合格时，系统必须携带评审反馈重生成该图，重生成次数不超过 BUDGET-2。
- BEH-4: 当图像生成调用发起时，系统必须携带固定种子与覆盖图内文字、水印、拼图、边框四类失败模式的负面提示词。
- BEH-5: 当尺码表详情图产出时，系统必须完全以本地渲染生成该图且不发起任何模型调用。
- BEH-6: 当图像或视频主路径失败时，系统必须退到单一兜底——图片为源图裁剪加本地合成，视频为图片轮播，不得尝试多级模型轮换。
- BEH-7: 当图片产物落盘时，系统必须校验主图白底方图最短边不低于 1200 像素、详情图最短边不低于 900 像素、单张不超过 4.5MB，不满足者被强制规格化处理。
- BEH-8: 当策略说明文档生成时，系统必须包含单商品成本估算、与人工流程的效率对比、换品类与换市场适配成本声明三节。

## IFACE 契约

- IFACE-1: 品类到构图模板的映射与市场到视觉风格条目的词典必须是产物包内独立配置资产，输入到输出的映射可枚举审查。
- IFACE-2: VLM 质检的评审结果必须是机器可解析的结构化记录——四维判定加问题清单——并留痕于运行日志；评审输入引用模型返回的图片 URL，符合模型到 URL 再到模型的链路约束。

## BUDGET 预算

- BUDGET-1: 视觉质量增强按一个 PR 一件事且 diff 低于 400 行的纪律逐卡拆分落地。
- BUDGET-2: 单张图质检不合格的重生成次数上限为 2 次，超限后走兜底路径。

## DECISION 决策

- DECISION-1: 本 spec 由 PM 自著（ADR-0085 决策 5 等价路径）——视觉质量策略属高决策密度面，不派低决策密度执行者。
- DECISION-2: 红队执行采用 CNB 免费沙盒攻击轮叠加 CI-Workflows adversary 审计回写（ADR-0067 语义，IR-0004 rev5 先例），沙箱产物一律机械核对——自报数字不采信，判定只认 GitHub 侧可回查的运行时证据。

## ASSUMPTION 假设

- ASSUMPTION-1: 图像生成模型与视觉评审模型在比赛窗口内持续可用且支持以 URL 引用图片输入（均在官方白名单 36 模型内）。
